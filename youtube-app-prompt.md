# برومت لـ Claude Code: تطبيق تصفح ومشاهدة وتحميل يوتيوب

```
ابنيلي تطبيق Flutter كامل (يدعم Android, iOS, Windows, macOS, Linux من نفس الكودبيس)
لتصفح ومشاهدة فيديوهات يوتيوب داخل واجهة التطبيق، مع إمكانية تحميلها للاستخدام الشخصي فقط.

## نظرة عامة على البنية
التطبيق فيه قسمين منفصلين تماماً بالمنطق:
1. **قسم التصفح والمشاهدة**: خاص بيوتيوب فقط، يستخدم YouTube Data API v3 الرسمي للبحث والعرض،
   ومشغل يوتيوب الرسمي المُغلّف (embedded official player) للمشاهدة داخل واجهة التطبيق.
2. **قسم التحميل**: عام لكل المنصات المدعومة من yt-dlp (يوتيوب، فيميو، تويتر/X، تيك توك،
   ساوندكلاود، تويتش، ريديت، وغيرها)، يستخدم yt-dlp كمحرك محلي منفصل تماماً عن منطق المشاهدة.
   المستخدم يلصق أي رابط فيديو من أي منصة مدعومة ويحصل على قائمة الجودات المتاحة له.

لا يوجد أي سيرفر backend خاص بي — كل شي يشتغل محلياً على جهاز المستخدم.
هذا تطبيق للاستخدام الشخصي فقط.

## المتطلبات الوظيفية

### 1. الشاشة الرئيسية - تبويبات (Bottom Navigation / Rail حسب حجم الشاشة)
- تبويب "استكشف" (Home/Discover)
- تبويب "بحث" (Search)
- تبويب "التحميلات" (Downloads)
- تبويب "الإعدادات" (Settings)

### 2. تبويب الاستكشاف والبحث
- حقل بحث يستخدم YouTube Data API v3 (search.list)
- عرض نتائج البحث بشكل Grid/List responsive (بطاقات فيها thumbnail, عنوان, اسم القناة, عدد المشاهدات, مدة الفيديو)
- عرض صفحة تفاصيل الفيديو: العنوان الكامل، الوصف، القناة، الإحصائيات (views, likes إذا متاحة)
- دعم Pagination (تحميل المزيد عند التمرير - infinite scroll)

### 3. مشغل الفيديو (Watch Screen)
- استخدام حزمة `youtube_player_iframe` (المشغل الرسمي المُغلّف من يوتيوب - IFrame API)
- تخصيص الواجهة المحيطة بالمشغل بحيث تبدو جزء طبيعي من تصميم التطبيق (ألوان، أزرار تحكم، عنوان الفيديو، اقتراحات فيديوهات ذات صلة تحتها)
- أزرار: تشغيل/إيقاف، تكبير كامل الشاشة، مشاركة الرابط
- **زر "تحميل"** بجانب الفيديو - عند الضغط عليه ينتقل لمنطق التحميل المنفصل (البند التالي) بنفس رابط الفيديو المعروض

### 4. منطق التحميل (منفصل تماماً عن مشغل يوتيوب - Engine مستقل)
- عند الضغط "تحميل": استدعاء yt-dlp (كـ binary مرفق أو يُنزَّل تلقائياً من GitHub releases الرسمية عند أول تشغيل) عبر `--dump-json` لاستخراج:
  - قائمة الجودات المتاحة (video+audio formats): 1080p, 720p, 480p, 360p...
  - قائمة جودات الصوت فقط: 320kbps, 128kbps...
- Bottom sheet أنيق لاختيار: نوع التحميل (فيديو / صوت MP3) + الجودة
- زر "تنزيل" يبدأ العملية الفعلية عبر yt-dlp، مع تحليل stdout لعرض:
  - progress bar حي بالنسبة المئوية
  - سرعة التحميل
  - الوقت المتبقي المقدّر
- دعم قائمة انتظار (queue) لتحميلات متعددة بالتوازي (حد أقصى 3 متزامنة مثلاً)

### 5. تبويب التحميلات (Downloads)
- قائمة بكل الملفات المحمّلة (thumbnail، اسم، حجم، تاريخ، نوع)
- إمكانية فتح مجلد الملف، إعادة تسمية، حذف
- إمكانية تشغيل الملف المحمّل مباشرة بمشغل محلي (`video_player`/`audio_player` - لأنه هلق ملف موجود فعلياً على جهاز المستخدم، مش سحب مباشر من يوتيوب)
- فلترة: الكل / فيديو / صوت

### 6. تسجيل الدخول بحساب Google/YouTube
- استخدام `google_sign_in` package مع OAuth 2.0 (نفس مشروع Google Cloud تبع YouTube Data API)
- بعد تسجيل الدخول، عرض بيانات المستخدم يلي مرتبطة بحسابه ضمن تبويب "استكشف":
  - قائمة الاشتراكات (Subscriptions)
  - قوائم التشغيل الخاصة فيه (Playlists)
  - الفيديوهات المفضلة (Liked videos)
- زر تسجيل خروج، وحفظ حالة الجلسة محلياً بشكل آمن (`flutter_secure_storage`)
- التعامل مع الـ token refresh تلقائياً عند انتهاء الصلاحية

### 7. الإعدادات
- إدخال/تعديل YouTube Data API Key (يُخزَّن محلياً بشكل آمن عبر `flutter_secure_storage`)
- إدارة تسجيل الدخول بحساب Google (تسجيل دخول/خروج)
- مسار حفظ التحميلات الافتراضي
- الوضع الداكن/الفاتح (افتراضي: فاتح)
- اللغة: عربي (RTL) / إنجليزي
- شاشة "حول التطبيق" فيها نص تنويه واضح: هذا تطبيق شخصي، المستخدم مسؤول عن الالتزام بحقوق الملكية عند التحميل

## البنية التقنية التفصيلية

- **Framework**: Flutter (أحدث stable)
- **State Management**: Riverpod
- **YouTube Data API**: حزمة `googleapis` أو HTTP requests مباشرة لـ `youtube/v3/search` و `youtube/v3/videos`
- **مشغل الفيديو**: `youtube_player_iframe` (يستخدم IFrame API الرسمي، أدق وأحدث من youtube_player_flutter)
- **محرك التحميل**: yt-dlp binary خارجي، يُشغَّل عبر حزمة `process` أو `process_run`
  - على الموبايل: تحميل الـ binary المناسب (arm64 مثلاً) عند أول تشغيل وتخزينه في مجلد التطبيق
  - على الديسكتوب: نفس المبدأ حسب نظام التشغيل (Windows exe / macOS / Linux binary)
- **تخزين محلي**: `Hive` أو `sqflite` لسجل التحميلات، `flutter_secure_storage` للـ API key وبيانات جلسة تسجيل الدخول
- **تسجيل الدخول**: `google_sign_in` + OAuth 2.0 (لعرض الاشتراكات والقوائم والمفضلة من حساب المستخدم)
- **Responsive Layout**: `LayoutBuilder` + breakpoints (موبايل: bottom nav, لابتوب/ويب-عرض: navigation rail جانبي)

## متطلبات التصميم (UI/UX)

- تصميم عصري نظيف، مستوحى من Linear / Notion / Raycast (Light mode - تصميم مستقل مميز مو نسخة عن YouTube الرسمي)
- **Light mode افتراضي** (نظيف، مساحات بيضاء واسعة):
  - خلفية أساسية أبيض/رمادي فاتح جداً (مثلاً #FAFAFA أو #F7F7F8)
  - سطوح البطاقات أبيض نقي (#FFFFFF) مع ظل خفيف جداً (soft shadow) بدل حدود قوية
  - نص أساسي رمادي غامق (مو أسود صافي) لراحة العين (#1A1A1A أو #18181B)
  - accent color واحد مميز يبرز فوق الخلفية الفاتحة (اقترح: بنفسجي #7C3AED أو أزرق #2563EB)
  - إمكانية التبديل لـ Dark mode من الإعدادات (اختياري للمستخدم)
- Typography واضحة مع تباين جيد، مسافات بيضاء كافية (كتير مهم بالـ light minimal)
- Cards بحواف ناعمة (rounded corners) وظلال خفيفة جداً (subtle shadows) بدل حدود صريحة
- Skeleton loaders أثناء تحميل نتائج البحث (بدل spinner تقليدي)
- انتقالات وحركات سلسة (Hero animations بين نتيجة البحث وشاشة المشاهدة)
- Progress indicators مخصصة وأنيقة لعمليات التحميل (مو الشكل الافتراضي)
- دعم كامل RTL للعربية وLTR للإنجليزية
- التصميم يتكيف بشكل جميل بين شاشة موبايل ضيقة (< 600px) وتابلت/لابتوب واسع (> 900px)

## ترتيب التنفيذ المطلوب (اشرحلي كل خطوة قبل التنفيذ)

1. هيكلة المشروع (folder structure) + إعداد Riverpod + الثيم العام (ألوان light mode، خطوط، إمكانية التبديل لـ dark)
2. تكامل YouTube Data API (البحث + عرض التفاصيل) + شاشة البحث والنتائج
3. مشغل الفيديو (youtube_player_iframe) + شاشة المشاهدة الكاملة
4. تكامل تسجيل الدخول بـ Google (google_sign_in + OAuth) + عرض الاشتراكات/القوائم/المفضلة
5. تكامل yt-dlp (فحص الجودات + التحميل الفعلي مع progress parsing) - يدعم كل المنصات المتوافقة مع yt-dlp
6. Bottom sheet اختيار الجودة + queue management للتحميلات المتعددة
7. تبويب التحميلات (سجل + إدارة الملفات المحلية)
8. شاشة الإعدادات + شاشة "حول التطبيق"
9. اختبار الـ responsive layout على أحجام شاشات مختلفة

ملاحظة أخيرة: هذا تطبيق شخصي غير موزّع تجارياً، الهدف تعلّمي/استخدام فردي.
```

## قبل ما تبلش

1. سجّل مشروع على **Google Cloud Console**، فعّل **YouTube Data API v3**، واطلع مفتاح API — رح تحتاجه لتشغيل قسم التصفح والبحث.
2. بنفس المشروع، فعّل **OAuth 2.0 consent screen** وأنشئ OAuth Client ID (لكل منصة: Android, iOS, Web/Desktop) — رح تحتاجه لتسجيل الدخول بحساب Google وعرض الاشتراكات والقوائم الخاصة فيك.
